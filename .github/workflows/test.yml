name: Ilk Test Pipeline

on:
    push:
        branches:
            - main
    workflow_dispatch:



jobs:
    bek_ilk_job:
        runs-on: ubuntu-latest
        steps:
          - name: ${{ github.repositoryUrl }}	
            run: echo "Hello World!"
